#include <stdio.h>
/**
 * main - entry point
 * description: printing of decimal
 * Return: always 0 zero
 */
int main(void)
{
	int n;

	for (n = 0; n <= 9; n++);
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
